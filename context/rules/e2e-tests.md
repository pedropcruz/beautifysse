# E2E / UI Testing Rules

## Framework
*   **Playwright**

## Mandates
1.  **Scope**: UI interactions, page navigation, and critical user flows.
2.  **Reliability**: Avoid flaky tests. Use locators resilient to change (e.g., `getByRole`, `getByTestId`).
